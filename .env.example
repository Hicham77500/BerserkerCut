# Variables d'environnement pour BerserkerCut

# Backend REST API (Node/Express + MongoDB)
EXPO_PUBLIC_API_BASE_URL=http://localhost:4000

# Forcer le mode démo (ignore l'API même si elle est configurée)
EXPO_PUBLIC_FORCE_DEMO_MODE=false

# App Configuration
APP_NAME=BerserkerCut
APP_VERSION=1.0.0
APP_ENVIRONMENT=development

# Analytics (optionnel)
GOOGLE_ANALYTICS_ID=
